[project]
name = "python-mud"
version = "0.1.0"
description = "人生游戏 MUD — Python asyncio 版"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "json-repair>=0.39.1",
]

[tool.uv]
package = false
